#include<iostream>
using namespace std;

int main()
{
    int x,i,n,ele[10];
    cin>>n;
    for(i=0; i<=n-1; i++)
    {
        cin>>ele[i];
    }
    for(i=n-1; i>=0; i--)
    {
        cout<<ele[i];
        cout<<" ";
    }
    return 0;
}
